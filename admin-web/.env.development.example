# Rename to `.env.development` (same folder as vite.config.js) if admin login fails.
# Use the exact host:port where the API answers (same as Swagger in the browser).
VITE_API_PROXY_TARGET=http://192.168.100.49:5119
